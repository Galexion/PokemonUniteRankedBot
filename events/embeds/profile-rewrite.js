const fs = require("fs");

module.exports = {
    name: 'profile',
    description: 'See your profile, and find others.',
    execute(message, args) {
        switch (args[0]) {
            case "search":

                break

            default:

                break
        }
    },
};